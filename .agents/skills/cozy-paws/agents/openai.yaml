interface:
  display_name: "Cozy Paws"
  short_description: "Repo-specific patterns and workflows for cozy-paws"
  default_prompt: "Use the cozy-paws repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true